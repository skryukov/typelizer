# Base partial for the composed-partial (named intersection) fixture —
# composed by composed_partials.json.jbuilder.

json.id course.id, typelize: "number"
json.title course.title, typelize: "string"
