# Rendered by the contract spec with NO jbuilder-inertia loaded: the
# `inertia:` kwarg must be stripped (with a one-time warning) so the render
# stays clean instead of crashing in jbuilder's `_extract_method_values`.
json.id 1
json.stats({total: 2}, inertia: :defer)
